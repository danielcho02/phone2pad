// Resource IDs shared by tray.rc and tray_main.cpp. A low ID so this icon is the one
// Explorer/Windows uses as the executable icon (the lowest-ID RT_GROUP_ICON wins).
#pragma once

#define IDI_PHONE2PAD 101
